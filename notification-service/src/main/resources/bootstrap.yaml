server:
  port: 7777
spring:
  application:
    name: notification-service
  cloud:
    config:
      fail-fast: true
      uri: http://config:8888
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
