server:
  port: 8080
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth-service
          uri: lb://auth-service
#          predicates:
#            - Path=/api/v1/auth/login, /api/v1/auth/validate, /api/v1/auth/
#            - Method=POST
        - id: cocktail-service
          uri: lb://cocktail-service

eureka:
  client:
    serviceUrl:
      defaultZone:
        #http://localhost:8761/eureka/
        #"http://${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka/"
        http://eureka:8761/eureka/
