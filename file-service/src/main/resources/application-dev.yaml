server:
  port: 8185
spring:
  cloud:
    openfeign:
      client:
        config:
          auth-service:
            url: http://localhost:8182/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'
